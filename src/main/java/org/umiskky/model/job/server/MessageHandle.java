package org.umiskky.model.job.server;

import org.umiskky.model.domain.Message;

/**
 * 处理message的线程
 */

public class MessageHandle{
    private  Message message;
    public void handle(){
/*        while (true){
            if (!MessageBuffer.messageLinkedList.isEmpty())
        message = MessageBuffer.messageLinkedList.peekFirst();
        }*/
    }
}
